<ol class='track-list'>
  <% track_list.each do |item| %>
    <li>
      <div class="<%= item[:status] || 'requested' %>">
          <span class="title"><%= item[:video].title %></span>
          <span class="requester">requested by <%= item[:user].email %></span>
      </div>
    </li>
  <% end %>
</ol>